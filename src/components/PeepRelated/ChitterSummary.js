import classes from "./ChitterSummary.module.css";

const ChitterSummary = () => {
	return (
		<section className={classes.summary}>
			<h2>Live Chitter Feed</h2>
			<p>Feel free to browse through the latest Peeps</p>
		</section>
	);
};

export default ChitterSummary;
